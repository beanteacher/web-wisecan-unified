# staging 프로파일 — CI/CD 파이프라인에서 SPRING_PROFILES_ACTIVE=staging 으로 기동
# develop 프로파일과 동일한 설정을 사용하되 명시적 staging 진입점을 제공한다
spring:
  config:
    import: "optional:application-develop.yml"

wisecan:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://dev.wisecan.com}
